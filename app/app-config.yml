
### DON'T add trailing slashes in dir names

# your text files folder absolute/relative path (to the src/app.py script)
datadir: /iexec


# If you're not sure do not edit this section, it's used by the app

input-config: input-config.yml    # text file names and params (voice, latency)
input-dir: in                     # this directory contains input files
output-dir: out                   # output directory